// file to hold the display view for various element positions

console.log("got here too");

// create main views for logo placement
var topView = Ti.UI.createView({
	top: 0
});

var logoView = Ti.UI.createView({
	bottom: 10,
	height: 50
});       
		        
// create display elements for current conditions
var list = Ti.UI.createLabel({
	  color: '#404040',
	  text: currentConditions.tempfDisplay,
	  height: Ti.UI.SIZE,
	  textAlign: 'center',
	  verticalAlign: Ti.UI.TEXT_VERTICAL_ALIGNMENT_CENTER,
	  font:{
	    fontSize: '55sp',
	    fontWeight: 'bold'
	}
});

